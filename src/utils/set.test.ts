// import { expect } from 'chai';
import set from './set';

describe('function set', () => {
  it('should throw an Error if path not a string', () => {});

  it.only('should return first param of it is not an object', () => {
    set(null, '', '');
    // expect(result).to.equal(null);
  });

  it('should throw an Error if path not a string', () => {});
});
